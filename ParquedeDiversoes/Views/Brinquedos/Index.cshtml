@model IEnumerable<ParquedeDiversoes.Models.Brinquedo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.manutencao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ultimaManutencao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.parque)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.manutencao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ultimaManutencao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.parque.ID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
